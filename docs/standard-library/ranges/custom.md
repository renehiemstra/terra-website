## Advanced Development

For extending the library:
- Use `rn.develop.RangeBase` to add base functionality to custom ranges.
- Factories: `rn.develop.factory.combiner`, etc., for creating new adapters/combiners.

See the source code in `range.t` for implementation details.